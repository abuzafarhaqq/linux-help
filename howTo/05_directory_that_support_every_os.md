# File Type That Support On Every Operating System ( Usefull In Dual Boot )

---

File System: `exFAT`

In ubuntu you need some packages for the support of exFAT. Install package via:

`sudo apt install dosfstools exfat-fuse exfat-utils`
